# BARS — Culture DJ + Mission Operator requirements
# Core (required on all platforms):
# (server.py uses only Python stdlib — no pip installs needed for chat + voice + missions)

# Screen takeover (hands.py) — Windows:
pyautogui>=0.9.54
mss>=10.2.0
Pillow>=12.3.0

# Desktop character (presence.py) — Windows/Linux:
PyQt6>=6.11.0

# Screen takeover (hands.py) — macOS (install pyobjc when needed):
# pyobjc-core>=10.0
# pyobjc-framework-Quartz>=10.0
# pyobjc-framework-ScreenCaptureKit>=10.0

# Optional — Claude Code CLI for build missions:
# Install separately: https://claude.com/claude-code
# Windows: npm install -g @anthropic-ai/claude-code
# macOS:   brew install claude-code  (or npm)
